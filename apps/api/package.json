{
  "name": "@confidential-indexer/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "tsx src/main.ts",
    "typecheck": "pnpm --filter @confidential-indexer/core build && pnpm --filter @confidential-indexer/db build && pnpm --filter @confidential-indexer/hyperindex-adapter build && pnpm --filter @confidential-indexer/zama build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@confidential-indexer/core": "workspace:*",
    "@confidential-indexer/db": "workspace:*",
    "@confidential-indexer/hyperindex-adapter": "workspace:*",
    "@confidential-indexer/zama": "workspace:*",
    "fastify": "^5.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
